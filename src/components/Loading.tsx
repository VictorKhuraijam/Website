import React from "react";

const Loading: React.FC = () => {
  return (
    <div className="inline-block w-4 h-4 ml-2 border-2 border-t-transparent border-white rounded-full animate-spin" />

  );
};

export default Loading;
